import React from 'react';
import './setting.styles.scss';

const Setting = () => {
    return (
        <div className="logContiner">
            setting
        </div>
    );
}

export default Setting;