import React from 'react';
import './footer.styles.scss';

const Footer = ({ Ref, scrollToMain }) => {
    return (
        <footer className='authHomeFooter' ref={Ref}>
            footer
        </footer>
    );
}

export default Footer;